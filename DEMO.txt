================================================================================
                    DEMO Y GUIA RAPIDA - legal-expand 1.5.2
          646 siglas legales espanolas + asistente BOE determinista
================================================================================

Este archivo acompana al notebook `legal_expand_demo.ipynb`.

No hay un `demo.py` separado en esta version: la demo mantenida es el notebook de
Colab y los ejemplos CLI/Python de este documento.


OPCION 1: GOOGLE COLAB
--------------------------------------------------------------------------------

Abre la demo interactiva:

  https://colab.research.google.com/github/686f6c61/pypi-legal-expand/blob/main/legal_expand_demo.ipynb

La primera celda instala la version exacta:

  !pip install -U legal-expand==1.5.2 -q

El notebook cubre expansion de siglas, diagnostico, auditoria, glosarios,
procesamiento batch, diccionarios personalizados, CLI y una matriz BOE de 25
casos con referencias resueltas, ambiguas, no soportadas, manuales, revision
explicada, HTML, informe por parrafos y plantilla de overrides.


OPCION 2: TERMINAL
--------------------------------------------------------------------------------

Instalar:

  python -m pip install -U legal-expand==1.5.2

Expandir texto desde stdin:

  printf "La AEAT gestiona el IVA segun el BOE.\n" | legal-expand

Expandir un archivo:

  legal-expand sentencia.txt --expand-only-first --output sentencia-expandida.txt

Generar auditoria:

  legal-expand audit sentencia.txt --report-format markdown --output audit.md

Generar glosario:

  legal-expand glossary sentencia.txt --glossary-format csv --output glosario.csv

Procesar una carpeta:

  legal-expand batch docs/ docs-expandidos/ --format html


OPCION 3: PYTHON
--------------------------------------------------------------------------------

Expansion basica:

  from legal_expand import expandir_siglas

  texto = "La AEAT gestiona el IVA segun el BOE."
  print(expandir_siglas(texto))

Salida estructurada:

  from legal_expand import ExpansionOptions, expandir_siglas

  resultado = expandir_siglas(
      "AEAT y BOE",
      ExpansionOptions(format="structured"),
  )
  print(resultado.to_json(indent=2))

Diagnostico de omisiones:

  from legal_expand import ExpansionOptions, expandir_siglas_detallado

  resultado = expandir_siglas_detallado(
      "Visita https://aeat.es y revisa AEAT y BOE",
      ExpansionOptions(include=["AEAT"]),
  )
  for omitida in resultado.omitted_acronyms:
      print(omitida.acronym, omitida.reason)


ASISTENTE BOE
--------------------------------------------------------------------------------

El asistente BOE no interpreta juridicamente el documento. Detecta referencias
explicitas, enlaza normas cuando hay seguridad suficiente y marca lo dudoso como
ambiguous, not-found, unsupported o needs-boe-search.

Modo offline, determinista y sin red:

  legal-expand boe sentencia.txt --output referencias-boe.md

Salida JSON:

  legal-expand boe sentencia.txt --report-format json --output referencias-boe.json

Revision humana explicada:

  legal-expand boe sentencia.txt --report-format review-json --output revision-boe.json

Informe HTML:

  legal-expand boe sentencia.txt --report-format html --output referencias-boe.html

Informe por parrafos:

  legal-expand boe sentencia.txt --report-format paragraphs --output sentencia-boe.md

Plantilla de overrides para completar pendientes:

  legal-expand boe sentencia.txt --overrides-template --output boe-overrides.template.json

Modo con cache y consulta opcional a BOE:

  legal-expand boe sentencia.txt --mode cache-first --timeout 4 --output referencias-boe.md

Python:

  from legal_expand import (
      boe_overrides_template,
      boe_report_by_paragraph_markdown,
      boe_report_to_html,
      boe_report_to_markdown,
      detectar_referencias_boe,
      revisar_boe,
  )

  texto = "El art. 24 de la Constitucion Espanola reconoce la tutela judicial."
  informe = detectar_referencias_boe(texto)
  print(boe_report_to_markdown(informe))
  print(revisar_boe(informe).to_json(indent=2))
  print(boe_report_to_html(informe))
  print(boe_report_by_paragraph_markdown(informe))
  print(boe_overrides_template(informe))


OVERRIDES MANUALES BOE
--------------------------------------------------------------------------------

Si el detector no puede resolver una referencia y la persona revisora si conoce
la norma correcta, se puede anadir un override JSON:

  {
    "references": [
      {
        "text": "la norma especial de informantes",
        "boe_id": "BOE-A-2023-4513",
        "title": "Ley 2/2023, de 20 de febrero",
        "unit": "articulo 42"
      }
    ]
  }

Uso:

  legal-expand boe informe.md --overrides boe-overrides.json --output referencias-boe.md

Las referencias manuales aparecen con estado `manual` para distinguirlas de lo
detectado automaticamente.

Si no sabes aun que poner en el override, genera primero una plantilla:

  legal-expand boe informe.md --overrides-template --output boe-overrides.template.json

La plantilla incluye solo referencias pendientes de revision, su texto original,
la unidad detectada si existe, candidatos cuando los haya y una nota explicativa.


CALIDAD Y VERIFICACION
--------------------------------------------------------------------------------

La release 1.5.2 mantiene la API de 1.5.1 y corrige la presentacion publica
de la documentacion, badges y demo reproducible.
Antes de publicarla hay que validar:

  - pytest con cobertura y umbral minimo 90%: OK
  - Bandit: sin issues identificados
  - build wheel/sdist: OK
  - twine check: OK
  - smoke install desde wheel: OK


RECURSOS
--------------------------------------------------------------------------------

PyPI:       https://pypi.org/project/legal-expand/
GitHub:     https://github.com/686f6c61/pypi-legal-expand
Colab:      https://colab.research.google.com/github/686f6c61/pypi-legal-expand/blob/main/legal_expand_demo.ipynb
npm:        https://www.npmjs.com/package/legal-expand
Licencia:   MIT

================================================================================
